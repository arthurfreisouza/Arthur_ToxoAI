
Everything checks out end to end. Here's a summary of what's in place.

Summary

Built the offline model strand for the local fine-tuning step (tasks.md T009, T049–T054), all now marked [X]:

- eval/split.py — regenerates the fixed 70/30 train/test split over the 24 historical records. Verified it reproduces the exact split documented in data-model.md: test = records 17, 5, 13, 21, 24; records 22/23 collapse into one tuple; unmeasured_classes = [1,3,7,16,18,20].
- eval/split.v1.json — the committed split artefact (18 distinct tuples → 13 train / 5 test).
- eval/tests/test_split.py — 8 invariant tests (disjoint, complete, tuple-level, stratified, byte-reproducible, etc.) — all passing.
- eval/dataset.py — emits the 19 training rows as chat prompt/completion pairs targeting only the classification labels (never argumentation/recommendation), with a hard leak-check against the held-out set.
- training_model/finetune_llama32_1b.ipynb — the fp16 LoRA fine-tuning notebook for Llama-3.2-1B-Instruct, ready to run top-to-bottom: loads the split/dataset without regenerating them, trains with completion-only loss, merges the adapter, saves locally, and pushes to a private HF repo.
- eval/ENDPOINT.md — HF Inference Endpoint config doc (scale-to-zero, greedy decoding).

Blocker you already know about: nvidia-smi/torch.cuda.is_available() can't see your RTX 2070 Mobile right now — the 595.84 driver is installed and DKMS-built, but the kernel module isn't loaded (Secure Boot is on, which usually means it needs a reboot to take effect). The notebook's first cell checks for this explicitly and refuses to run on CPU rather than silently doing the wrong thing. Once you reboot and nvidia-smi shows the GPU, run pip install -r training_model/requirements.txt (plus a CUDA-matched torch build) and execute the notebook — that will actually produce the fine-tuned model.
